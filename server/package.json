{
  "scripts": {
    "start": "node build/server/src/server.js",
    "dev": "rm -rf build && tsc && tsc-alias && node build/server/src/server.js",
    "build": "rm -rf build && tsc && tsc-alias"
  },
  "type": "module",
  "devDependencies": {
    "tsc-alias": "^1.8.5",
    "typescript": "^5.0.3"
  },
  "dependencies": {
    "@types/express": "^4.17.17",
    "@types/matter-js": "^0.18.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "http": "^0.0.1-security",
    "matter-js": "^0.19.0",
    "socket.io": "^4.6.1"
  }
}
